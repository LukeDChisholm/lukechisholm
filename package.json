{
  "name": "app",
  "private": true,
  "devDependencies": {
    "esbuild": "^0.28.1"
  },
  "scripts": {
    "build": "esbuild ./app/javascript/entrypoints/application.js --bundle --outdir=app/assets/builds"
  }
}
